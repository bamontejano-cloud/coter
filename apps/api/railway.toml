[build]
builder = "nixpacks"
buildCommand = "npm install --prefer-offline && npx prisma generate && npm run build"

[deploy]
startCommand = "npx prisma db push --accept-data-loss && node dist/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
