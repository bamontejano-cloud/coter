# Copy this to Railway environment variables (Settings → Variables)
# Do NOT commit real values to git

DATABASE_URL="postgresql://user:password@host:5432/coterapeuta_prod"
JWT_SECRET="replace-with-at-least-32-random-chars-use-openssl-rand-base64-32"
NODE_ENV="production"
PORT="3000"
ALLOWED_ORIGIN="https://your-app.vercel.app"
