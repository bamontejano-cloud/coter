[build]
builder = "nixpacks"
buildCommand = "npm install --prefer-offline && npm run build --workspace=apps/web"

[deploy]
startCommand = "npx serve -s apps/web/dist -l tcp://0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
