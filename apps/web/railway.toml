[build]
builder = "nixpacks"
buildCommand = "npm install --prefer-offline && cd apps/web && npm run build"

[deploy]
startCommand = "cd apps/web && ./node_modules/.bin/serve -s dist --listen $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
